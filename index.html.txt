<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<title>سنتر أ/ أحمد الشرقاوي</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
body{
  font-family: Arial, sans-serif;
  margin:0;
  background:#f2f2f2;
  direction: rtl;
}

/* NAV */
nav{
  background:#4b3cc4;
  display:flex;
  justify-content:space-around;
  padding:10px;
}
nav button{
  background:none;
  border:none;
  color:white;
  font-size:14px;
  cursor:pointer;
}

/* SECTIONS */
section{
  display:none;
  padding:20px;
}
section.active{
  display:block;
}

h1,h2{
  text-align:center;
}

.card{
  background:white;
  padding:15px;
  border-radius:10px;
  margin-bottom:15px;
}

input,select,button{
  width:100%;
  padding:10px;
  margin:5px 0;
}

button.main{
  background:#4b3cc4;
  color:white;
  border:none;
  border-radius:8px;
}

ul{
  list-style:none;
  padding:0;
}
li{
  background:#eee;
  padding:10px;
  margin:5px 0;
  border-radius:8px;
}
</style>
</head>

<body>

<h1>📘 سنتر أ/ أحمد الشرقاوي</h1>

<nav>
  <button onclick="showSection('students')">➕ الطلاب</button>
  <button onclick="showSection('attendance')">📷 الحضور</button>
  <button onclick="showSection('exams')">📝 امتحانات</button>
  <button onclick="showSection('evaluation')">⭐ تقييم</button>
  <button onclick="showSection('profile')">👤 ملف الطالب</button>
</nav>

<!-- الطلاب -->
<section id="students" class="active">
  <h2>إضافة طالب</h2>
  <div class="card">
    <input id="studentName" placeholder="اسم الطالب">
    <select id="group">
      <option value="">اختر المجموعة</option>
      <option value="الأولى">أولى ثانوي - المجموعة الأولى</option>
      <option value="الثانية">أولى ثانوي - المجموعة الثانية</option>
    </select>
    <button class="main" onclick="addStudent()">إضافة</button>
  </div>

  <ul id="studentsList"></ul>
</section>

<!-- الحضور -->
<section id="attendance">
  <h2>الحضور والغياب</h2>
  <div class="card">
    <p>⬅️ سجلي الحضور من صفحة الطلاب</p>
    <p>🕒 الغياب التلقائي هيتضاف في الخطوة الجاية</p>
  </div>
</section>

<!-- الامتحانات -->
<section id="exams">
  <h2>امتحانات اختيار من متعدد</h2>
  <div class="card">
    <p>📌 تجهيز الامتحان (قريبًا)</p>
  </div>
</section>

<!-- التقييم -->
<section id="evaluation">
  <h2>تقييم الطالب</h2>
  <div class="card">
    <p>📌 تقييم ومستوى الطالب (قريبًا)</p>
  </div>
</section>

<!-- ملف الطالب -->
<section id="profile">
  <h2>ملف الطالب</h2>
  <div class="card">
    <p>📌 كل بيانات الطالب هتظهر هنا</p>
  </div>
</section>

<script>
let students = JSON.parse(localStorage.getItem("students")) || [];

function showSection(id){
  document.querySelectorAll("section").forEach(s=>s.classList.remove("active"));
  document.getElementById(id).classList.add("active");
}

function addStudent(){
  let name = studentName.value;
  let group = document.getElementById("group").value;

  if(name=="" || group==""){
    alert("كملي البيانات");
    return;
  }

  students.push({
    id: Date.now(),
    name,
    group,
    attendance: [],
    exams: [],
    evaluation: ""
  });

  localStorage.setItem("students", JSON.stringify(students));
  studentName.value="";
  group.value="";
  renderStudents();
}

function markAttendance(studentId){
  let today = new Date().toLocaleDateString("ar-EG");
  let student = students.find(s => s.id === studentId);
  if(!student) return;

  let exists = student.attendance.find(a => a.date === today);
  if(exists){
    alert("تم تسجيل الحضور قبل كده");
    return;
  }

  student.attendance.push({
    date: today,
    status: "حاضر"
  });

  localStorage.setItem("students", JSON.stringify(students));
  alert("✅ تم تسجيل الحضور");
}

function renderStudents(){
  studentsList.innerHTML="";
  students.forEach(s=>{
    let li=document.createElement("li");
    li.innerHTML = `
      <b>${s.name}</b><br>
      المجموعة: ${s.group}<br>
      <button onclick="markAttendance(${s.id})">تسجيل حضور</button>
    `;
    studentsList.appendChild(li);
  });
}

renderStudents();
</script>

</body>
</html>
